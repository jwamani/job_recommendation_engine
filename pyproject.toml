[project]
name = "job-recommendation-engine"
version = "0.1.0"
description = "A job recommendation engine built with Django."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.7",
    "django-filter>=26.1",
]
